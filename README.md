# Footy-Trivia

- simple Python web app (Flask/FastAPI/Django)
  * SQL Database (ORM)
  * Redis cache
  * nginx proxy
  * dockerized

- CI/CD pipeline
  * automatically build on push
  * deploy automatically to server

- server deployment
  * automated HTTPS certificates (letsencrypt)
  * server hardening
  * bonus: IaC/Ansible
